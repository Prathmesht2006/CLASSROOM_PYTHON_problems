# 6. WAP to remove all duplicates from a given list of strings and return a list of unique strings

lst = ["apple", "banana", "apple", "orange", "banana"]

unique_list = list(set(lst))
print(unique_list)
